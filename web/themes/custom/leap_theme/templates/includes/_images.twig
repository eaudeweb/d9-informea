{% macro get(image, theme_path, width, height, class) %}
  <img class="{{ image|lower|clean_class }} {{ class|default('') }}" width="{{ width|default(100) }}" height={{ height|default(100) }} src="{{ theme_path ~ '/images/' ~  image }}" />
{% endmacro %}
